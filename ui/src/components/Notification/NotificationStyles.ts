import NotificationsIcon from '@mui/icons-material/Notifications'
import { styled } from '@mui/material/styles'

export const CustomNotificationsIcon = styled(NotificationsIcon)(() => `
	cursor: pointer;
`)